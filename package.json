{
  "name": "blockchain-sdk",
  "version": "1.0.0",
  "description": "SDK for interacting with PUGG's smart contracts via web3",
  "main": "index.js",
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish"
  },
  "author": "Pugg Dev",
  "license": "ISC",
  "dependencies": {
    "eth-hd-wallet": "^0.5.1",
    "ganache-cli": "^6.12.2",
    "node-fetch": "^3.1.0",
    "web3": "^1.6.1",
    "web3-utils": "^1.6.1"
  }
}
